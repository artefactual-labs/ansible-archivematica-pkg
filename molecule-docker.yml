---
driver:
  name: docker

docker:
  containers:

      - name: archivematica-trusty
        image: ubuntu
        image_version: trusty
        ansible_groups:
          - group1

      - name: archivematica-centos7
        image: centos
        image_version: 7
        ansible_groups:
          - group1

verifier:
  name: testinfra
